trigger:
- master
  
stages:
 - stage: Deploy_stage
   displayName: Deploy_stage
   jobs:
    - deployment:
      environment: Chat-app-env
      strategy:
        runOnce:
          deploy:
            steps:
              - script: |
                  echo $(BuildDirectory)
                  #sudo npm install
                  echo 'npm installed!'
                  #sudo npm run
                  #sudo npm run test --if-present
